{
  "name": "securebank-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "SecureBank - Production Quality Simulated Indian Internet Banking Portal",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "build": "npm run build:server && npm run build:client",
    "db:reset": "node server/src/database/reset_users.js",
    "test:integration": "node server/src/test_suite.js",
    "start": "node server/dist/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
